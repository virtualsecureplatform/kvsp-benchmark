# kvsp-benchmark

CPU mode

```
./benchmark.sh
```

GPU mode

```
./benchmark.sh -g number-of-GPUs-you-have
```

# Dependency

Ubuntu

```
sudo apt update
sudo apt install ruby libgoogle-perftools-dev libomp-dev
```
